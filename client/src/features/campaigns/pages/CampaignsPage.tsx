import AppLayout from '@/app/layouts/AppLayout'

export default function CampaignsPage() {
  return (
    <AppLayout>
      <h1 className="font-display font-bold text-3xl mb-2">Campaigns</h1>
      <p className="text-ink/50">Bulk notification campaigns — V2 feature, built after MVP.</p>
    </AppLayout>
  )
}